#include<stdio.h>
int main()
  {
  int i,n,a[10];
  printf("Enter the number:");
  scanf("%d",&n);
  for(i=0;i<=n;i++)
    {
     scanf("%d",&a[i]);
     }
     printf("The Total number is:%d",n);
     return 0;
    } 
